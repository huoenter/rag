# Exam Review and Teams in Lab 4

---
# Patterns Covered

* Strategy
* Reader
* Observer
* Decorator

---
# Strategy

* Lab 2
  * UML, context
* Example in lecture notes
  * UML, context
* Generic version

---
# Reader

* Definition
  * the interface
* why do we need `flatMap`
* Connection between Strategy
  * taking care of dependency

---
# Observer

* Signal-counter example
* Spreadsheet example
* NOT the weather station example
* Generic version not require in the exam

---
# Decorator

* Lecture notes and text book example
  * UML, context
  * variants like the one in the sample exam

---
# Remote

* `git remote add <name> <url>`
* `git remote`
* `git remote -v`
* `git push <name> <branch>`
  * `master`

---
# Workflow

* How we use `git` is called `workflow`
* We will use a basic one called `Centralized Workflow`
  * The other common one is `Feature Branch Workflow`
* Example

---
# Notes

* To have less conflicts:
  * Always `pull` before you start working
  * Divide the work more reasonably
* Use `git push -f` as the last resort
