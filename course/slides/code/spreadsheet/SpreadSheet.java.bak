import java.util.List;
import java.util.ArrayList;

public class SpreadSheet {

  public static void main(String[] args) {
    SimpleCell c1 = new SimpleCell("C1");
    SimpleCell c2 = new SimpleCell("C2");
    c1.addObserver(c2);
    c1.update(10);

    /*
    ArithmeticCell c3 = new ArithmeticCell("C3");

    c1.addObserver(c3::setLeft);
    c2.addObserver(c3::setRight);
    
    c1.update(10);
    c2.update(20);

    ArithmeticCell c4 = new ArithmeticCell("C4");
    
    c1.addObserver(n -> c4.setLeft(n));
    c3.addObserver(n -> c4.setRight(n));
    
    System.out.println("update c1 to 15");
    c1.update(15);

    System.out.println("update c3 to 33");
    c3.update(33);
    */
  }
}

interface Observer<A> {
  void update(A a);
}

interface Subject<A> {
  void addObserver(Observer<A> o);
  void notifyObservers();
}


class ArithmeticCell extends SimpleCell {
  private int left, right;

  public ArithmeticCell(String s) {
    super(s);
  }

  public ArithmeticCell(String s, SimpleCell s1, SimpleCell s2) {
    super(s);
    s1.addObserver(n -> setLeft(n));
    s2.addObserver(n -> setRight(n));
  }
  
  void setLeft(int n) {
    left = n;
    update(left+right);
  }
  
  void setRight(int n) {
    right = n;
    update(left+right);
  }
  
}

class SimpleCell implements Subject<Integer>, Observer<Integer> {
  private String name;
  private int val;
  private List<Observer<Integer>> obs;
  
  public SimpleCell(String s) {
    name = s;
    obs = new ArrayList<>();
    val = 0;
  }

  @Override
  public void update(Integer a) {
    val = a;
    System.out.println(name + ":" + val);
    notifyObservers();
  }

  @Override
  public void addObserver(Observer<Integer> o) {
    obs.add(o);
  }

  @Override
  public void notifyObservers() {
    obs.forEach(ob -> ob.update(val));
  }
}
